# SPDX-License-Identifier: LGPL-2.1-or-later

# tlp looks for drop-ins in /etc/tlp.d, point it at the extension.
# Machine local overrides still work through /etc/tlp.conf, which
# wins over the drop-ins and stays writable.
L /etc/tlp.d - - - - ../usr/lib/tlp.d
