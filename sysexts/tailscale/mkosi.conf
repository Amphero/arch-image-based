# SPDX-License-Identifier: LGPL-2.1-or-later

# Tailscale client. The daemon is enabled statically via the extra
# tree, log in once with tailscale up.

[Distribution]
Distribution=arch

[Output]
Format=sysext
Overlay=yes

[Content]
Packages=
        tailscale
