# SPDX-License-Identifier: LGPL-2.1-or-later

[UKIProfile]
Profile=
        ID=install
        TITLE=Installer

Cmdline=
        root=tmpfs
        mount.usr=dissect
        rd.systemd.mask=systemd-repart.service
        systemd.mask=systemd-repart.service
        systemd.unit=system-install.target
        systemd.set-credential=passwd.plaintext-password.root:arch
        rw
        audit=0
        systemd.image_policy=esp=unprotected:xbootldr=unprotected+unused+absent:usr=signed:=ignore
        systemd.image_filter=usr=ArchLinux_*:usr-verity=ArchLinux_*:usr-verity-sig=ArchLinux_*
        ipe.enforce=0

SignExpectedPcr=no
