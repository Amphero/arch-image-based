# SPDX-License-Identifier: LGPL-2.1-or-later
# Workspace for the weekly self rebuild: repo checkout, package cache,
# build output, signing key. Survives a factory reset on purpose.
[Partition]
Type=linux-generic
Label=%M-builder
Format=btrfs
SizeMinBytes=24G
SizeMaxBytes=24G
Encrypt=tpm2
