# SPDX-License-Identifier: LGPL-2.1-or-later

# Adds the live and installer entries to the boot menu. Only the image
# that gets burned to a stick needs them, an installed system does not.

[Content]
UnifiedKernelImageProfiles=uki/10-live.conf,uki/20-install.conf
