# SPDX-License-Identifier: LGPL-2.1-or-later
# The first boot wizard (user creation) talks on the console. Let it
# finish before the display manager takes over the seat. On every later
# boot the wizard is skipped and this costs nothing.
[Unit]
After=systemd-homed-firstboot.service
